/**
 * Logo size configuration.
 *
 * Each key is the filename (without extension) of a logo in the "Logo" folder
 * on Vercel Blob.  The value is a scale percentage where 100 = default size.
 *
 * Example:
 *   "nike"  → 120  → 20% larger than default
 *   "sony"  →  80  → 20% smaller than default
 *
 * Logos not listed here default to 100 (no change).
 */
export const LOGO_SCALES: Record<string, number> = {
    // Add your logos here. Use the exact filename (without .svg / .png / .webm).
    // Example:
    "nike 250": 150,
    "adidas": 85,
    "logo": 140,
    "air-jordan 250": 110,
};
